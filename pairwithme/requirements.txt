# Django 1.11 LTS. Bumped from 1.11.2 to the final 1.11.x patch (1.11.29):
# Python 3.7 (the newest interpreter Django 1.11 supports) needs the 3.7
# compatibility fixes that first shipped in 1.11.17.
Django==1.11.29
django-webpack-loader==0.5.0
djangorestframework==3.6.3
# mysqlclient 1.4.6 keeps the `bytes` encoder and version metadata Django 1.11's
# MySQL backend expects (2.x drops them). It builds against the MySQL 8.0 client
# headers (libmysqlclient-dev) the install script provides; the newer MariaDB
# Connector/C headers dropped the `MYSQL.reconnect` member 1.4.6 references.
mysqlclient==1.4.6
Pygments==2.2.0
pytz==2017.2
