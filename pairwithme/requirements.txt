# Django 1.11 LTS. Pinned to the final 1.11.x patch release (1.11.29) instead of
# 1.11.2 because Python 3.7 (the newest interpreter Django 1.11 supports) requires
# the Python 3.7 compatibility fixes that first shipped in 1.11.17.
Django==1.11.29
django-webpack-loader==0.5.0
djangorestframework==3.6.3
# mysqlclient 1.3.6 cannot compile against modern MariaDB/MySQL client libraries.
# 1.4.6 is the newest 1.4.x release: it builds cleanly against the modern client
# and stays compatible with Django 1.11's MySQL backend (2.x changed the encoders
# in a way Django 1.11 does not support).
mysqlclient==1.4.6
Pygments==2.2.0
pytz==2017.2
